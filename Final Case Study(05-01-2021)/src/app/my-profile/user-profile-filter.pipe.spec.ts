import { UserProfileFilterPipe } from './user-profile-filter.pipe';

describe('UserProfileFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new UserProfileFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
